<%= form_for ([@concert, @ticket]) do |f| %>
    
    <%= f.text_area :notes %>

    <%= f.hidden_field :concert_id, :value => @concert %>

    <%= f.hidden_field :user_id, :value => @user %>
   
    <%= f.submit %>
  <% end %>